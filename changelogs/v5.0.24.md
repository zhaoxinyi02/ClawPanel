## v5.0.24 - 修复 QQ 唤醒配置不生效（Issue #21）

### Bug 修复
- **fix**: 修复 QQ (NapCat) 通道配置中 `wakeProbability`、`wakeTrigger` 等参数仅在面板展示但未实际生效的问题
- **fix**: QQ 通道配置页面字段改为插件真实配置路径：
  - `rateLimit.wakeProbability`
  - `rateLimit.minIntervalSec`
  - `rateLimit.wakeTrigger.keywords`
  - `autoApprove.group.enabled` / `autoApprove.friend.enabled`

### 兼容性改进
- **feat**: 后端 `SaveChannel` 对 QQ 通道新增旧字段兼容迁移，自动将历史字段转换为新版结构并写入 `openclaw.json`
  - `wakeProbability` -> `rateLimit.wakeProbability`
  - `minSendIntervalMs` -> `rateLimit.minIntervalSec`
  - `wakeTrigger` -> `rateLimit.wakeTrigger.keywords`
  - `autoApproveGroup/Friend` -> `autoApprove.group/friend.enabled`
