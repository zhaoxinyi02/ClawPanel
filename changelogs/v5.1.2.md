## v5.1.2 - Agents 弹窗结构化配置与 implicit main 语义修复

### 重点改进
- **feat**: `Agents` 新建/编辑弹窗升级为结构化配置器
  - 分为 `Basic / Behavior / Access & Safety / Collaboration / Advanced`
  - 首屏改为任务导向表单，不再默认暴露大段 JSON
  - 增加默认模型提示、快捷模型 chips、默认 Agent 行为说明
- **feat**: 弹窗 header / footer 改为 sticky 布局
  - 1366×768 笔记本视口下，保存按钮保持可见
  - 保存前给出更明确的阻塞原因与引导文案

### 语义修复
- **fix**: implicit `main` 占位 Agent 现在有明确语义
  - 后端 `GET /api/openclaw/agents` 会为运行时合成的占位项返回 `implicit=true`
  - 前端据此显示 `PLACEHOLDER`、使用“配置”而不是“编辑”，并禁用删除
  - 当用户首次保存 `main` 时，会正确物化为显式 Agent；保存后自动切回普通“编辑 / 删除”状态
  - 当 `agents.list` 缺失但磁盘上已发现 agent 目录时，这些发现项也会按 implicit Agent 处理，避免直接 update/delete 命中 404
  - legacy single-agent 模式显式返回 `implicit=false`，不会被误判成 placeholder
- **fix**: Advanced JSON 不再因结构化保存而静默丢失未知字段
  - 仅在用户真正改动对应结构化分组时，才覆盖 `model / params / identity / groupChat / tools / subagents`
  - 未触碰的高级 JSON 会原样保留

### 测试
- **test**: 新增
  - `TestGetOpenClawAgentsMarksImplicitMainPlaceholder`
  - `TestGetOpenClawAgentsKeepsExplicitMainNonImplicit`
- 已通过：`go test ./...`
- 已通过：`npm --prefix web run build`
- 已通过：隔离 `OPENCLAW_DIR` 的 Playwright 实机验证（implicit `main` 物化、Advanced JSON 保留、1366×768 保存按钮可见）

### 影响说明
- 前端现在会把“运行时占位 Agent”和“显式配置 Agent”区分显示，避免用户误以为占位 `main` 已经写入 `agents.list`。
- 如果你仅修改 Basic 信息而不触碰 Advanced 对应结构化分组，原有高级字段会继续保留，不需要重新手写 JSON。

### CI / 运行时加固
- **fix**: 恢复跟踪 `cmd/clawpanel/frontend/dist/.keep`
  - 修复 clean checkout 下 `//go:embed all:frontend/dist` 因目录不存在导致的 `go vet ./...` / `go test ./...` 失败
  - GitHub Actions 不再依赖本地残留 dist 文件即可通过 Go 侧校验
- **fix**: 合并 upstream/main 后补齐多处运行时修复
  - 同步最新 `config` 路径探测与 PATH/HOME 相关稳定性修复到 `multi-agent`
- **fix**: QQ access token 同步链路加固
  - `ReadQQChannelState()` 统一复用 JSON/JSON5 解析，避免 NapCat 诊断/修复在 JSON5 配置下读取空 token
  - NapCat 安装脚本改为从当前配置目录读取 token，并以 base64 + JSON 编码安全写入 `onebot11.json`
  - OneBot11 事件监听器在重连时会重新读取最新 QQ token，避免 token 轮换后必须重启面板
- **fix**: OpenClaw 配置 schema 兼容性修复
  - 停止把默认 Agent 写成已废弃的 `agents.default`，改为写入 `agents.list[].default=true`
  - `Agents` 页面 sandbox 起步模板改为当前官方 schema（`mode: all/off` + `workspaceAccess: ro/rw`）
  - 写回 `openclaw.json` 时会自动把旧的 `workspace-write/read-only/danger-full-access` 迁移到当前 OpenClaw 可接受的字段组合
