# ClawPanel v4.3.0 发布：技能插件分离 · 一键更新实时日志 · 修改密码 · 多语言 · 原生安装

> ClawPanel 是 OpenClaw AI 助手的可视化管理面板，支持 QQ/微信/飞书/钉钉等 20+ 通道接入，多模型配置，技能管理，定时任务等。本次 v4.3.0 是一次功能大更新。

## 🎯 本次更新亮点

### 1. 技能中心全面升级 — 技能/插件分离

之前版本中，技能和插件混在一起显示，用户很难区分哪些是 AI 技能、哪些是通道插件。

**v4.3.0 改进：**
- 技能中心拆分为 **「技能」「插件」「ClawHub 商店」** 三个独立标签页
- 技能页显示所有 AI 技能（内置 52 个 + 工作区 + 脚本），支持搜索筛选
- 插件页独立管理飞书/钉钉/企业微信/QQ官方Bot 等通道插件
- 修复了 Docker 环境下无法扫描应用内置技能的 Bug（之前只显示 13 个，现在正确显示 65+ 个）

> 📌 **技术细节**：新增 `OPENCLAW_APP` 环境变量，Docker 容器通过只读卷挂载宿主机的 OpenClaw 应用目录，后端扫描多个候选路径确保兼容性。

### 2. 版本更新实时状态 — 终于能看到更新进度了！

之前点击「立即更新」后，只能看到一句「正在更新」，完全不知道更新到哪了、成功没有。

**v4.3.0 改进：**
- 点击更新后，实时显示 **终端风格的日志面板**（黑底彩色文字）
- **自动滚动**：新日志自动滚动到底部
- **彩色状态**：✅ 成功绿色、❌ 失败红色、⏳ 等待蓝色
- **计时器**：显示已用时间（如 `15s`、`1m30s`）
- **状态指示器**：正在更新（蓝色旋转）/ 更新完成（绿色勾）/ 更新失败（红色叹号）
- 即使没检测到新版本，也提供 **「强制更新」** 按钮

> 📌 **技术实现**：容器内写入信号文件 → 宿主机 `update-watcher.sh` 脚本监听并执行 `openclaw update` → 实时写入日志文件 → 前端每秒轮询读取日志并渲染。

### 3. 修改管理密码

终于可以在面板里直接修改登录密码了！

- 路径：系统配置 → 通用 → 修改管理密码
- 需要输入旧密码验证身份
- 修改成功后自动退出登录，用新密码重新登录

### 4. 多语言支持

- 支持 **中文（简体）** 和 **English** 两种语言
- 侧边栏底部一键切换
- 所有页面文案均已翻译

### 5. 原生安装方式

除了 Docker，现在还提供三种原生安装方式：

```bash
# Linux 一键安装
curl -fsSL https://raw.githubusercontent.com/zhaoxinyi02/ClawPanel/main/install.sh | bash

# macOS
curl -fsSL https://raw.githubusercontent.com/zhaoxinyi02/ClawPanel/main/install-mac.sh | bash

# Windows PowerShell
irm https://raw.githubusercontent.com/zhaoxinyi02/ClawPanel/main/install.ps1 | iex
```

自动检测 OpenClaw 路径、创建系统服务、开机自启。

### 6. 在线演示站

访问 [demo.zhaoxinyi.xyz](https://demo.zhaoxinyi.xyz) 即可体验完整功能，使用模拟数据，无需部署。

## 🔧 Bug 修复

- **技能扫描**：修复 Docker 环境下只显示 13 个技能的问题（实际有 65+）
- **NapCat 认证**：修复 `ADMIN_TOKEN ≠ WEBUI_TOKEN` 时 QQ 登录 Unauthorized
- **QQ 进程崩溃**：QQ 进程崩溃后自动重启，不再导致整个容器重启
- **NapCat 认证重试**：API 调用失败时自动重新认证并重试

## 📦 升级方式

```bash
# Docker 用户
cd ClawPanel
git pull
docker compose up -d --build

# 原生安装用户
openclaw update
```

## 🔗 相关链接

- **GitHub**：[github.com/zhaoxinyi02/ClawPanel](https://github.com/zhaoxinyi02/ClawPanel)
- **在线演示**：[demo.zhaoxinyi.xyz](https://demo.zhaoxinyi.xyz)
- **OpenClaw 官网**：[openclaw.ai](https://openclaw.ai)

---

**如果觉得有用，请给个 ⭐ Star 支持一下！**

标签：`OpenClaw` `AI助手` `QQ机器人` `微信机器人` `管理面板` `Docker` `React` `TypeScript` `开源`
